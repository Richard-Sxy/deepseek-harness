# 这边处理的是边界问题

## 失败分类和内容提示

| 失败情况 | 返回给模型的内容 |
|---|---|
工具执行抛出异常 ｜ 捕获一场，转化为`isError: true`的工具结果，交给模型决定下一步
工具不存在或者不可访问 ｜ 返回位置工具错误，由模型重新选择工具
调用参数不能无损序列化为 JSON ｜ 返回参数错误，模型修正后重新调用
执行前被权限活策略拒绝 ｜ 不执行工具，返回拒绝原因，不自动提升权限
工具输出校验失败 ｜ 将本次掉调用转换为失败结果，返回错误
执行中被取消 ｜ 通过取消信号通知工具，等待已启动的执行结束后收尾
